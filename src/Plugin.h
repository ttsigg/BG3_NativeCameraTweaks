#pragma once

// Normally generated from cmake/Plugin.h.in by CMake. Checked in for the Linux
// build (which globs sources instead of running the powershell sourcegen).
namespace Plugin
{
	inline constexpr auto NAME = "BG3NativeCameraTweaks"sv;
	inline constexpr auto AUTHOR = ""sv;
	inline constexpr auto Version =
		2u * 10000 +
		4u * 100 +
		5u;
}
